<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.codnate3">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".fragment.Bad_Codnate_view" />
        <activity android:name=".tuuti" />
        <activity android:name=".intent.MyPage" />
        <activity android:name=".intent.Start_Mens" />
        <activity android:name=".intent.Start5" />
        <activity android:name=".intent.Start4" />
        <activity android:name=".intent.Start3" />
        <activity android:name=".intent.Start2" />
        <activity android:name=".intent.Start1" />
        <activity android:name=".intent.camera" />
        <activity android:name=".intent.Image_detail" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>